apiVersion: apps/v1
kind: Deployment
metadata:
  name: discern-entity-platform
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: go-backend
        image: gcr.io/discern/entity-platform:latest
        ports:
        - containerPort: 8080
